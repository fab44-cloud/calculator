function add(a,b) {
    return a + b;
}

// add(4,6);

function subtract(a,b) {
    return a - b;
}

// subtract(10,4);

function multiply(a,b) {
    return a * b;
}

// multiply(4,4);

function divide(a,b) {
    return a / b;
}

// divide(10, 5);